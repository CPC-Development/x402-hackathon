# Copy to ports.env and adjust PORT_OFFSET or explicit ports.
# Offsets are applied by running ./gen-ports-env.sh
PORT_OFFSET=0

HARDHAT_PORT=8545
FACILITATOR_PORT=8080
SEQUENCER_PORT=4001
SERVICE_PORT=4000
